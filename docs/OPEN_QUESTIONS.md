# Open Questions

1. The SDD does not define a concrete schema for `contracts/metrics_catalog.json` (fields, units, or types). The current file uses a minimal `id` / `category` / `description` shape. Should this align to a specific external schema or include units/types?
